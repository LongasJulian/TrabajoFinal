<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.longasjulian.form_regis_lb1">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <application
        android:name=".ui.main.Form_regis_LB1"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmpleadosJefesActivity">
        </activity>
        <activity
            android:name=".MiPerfilActivity"
            android:label="@string/Mi_perfil" />
        <activity android:name=".AgregarHorarioActivity" />
        <activity android:name=".AgregarAnunciosActivity" />
        <activity
            android:name=".MainActivityJefe"
            android:label="@string/title_activity_main_jefe"
            android:theme="@style/AppTheme.DarkActionBar" />
        <activity
            android:name=".MainActivityEmpleado"
            android:label="@string/title_activity_main3"
            android:theme="@style/AppTheme.DarkActionBar" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity2" />
        <activity android:name=".RegisterActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>